Legacy internal API compatibility paths for Blizzway.

These routes remain under `/api/career7/*` only so existing Blizzway frontend
calls keep working while BGOS adds native `/api/blizzway/*` routes. Do not use
Career7 as public product copy, SEO text, domain text, or support copy.
