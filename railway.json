{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "preDeployCommand": "npm run prisma:migrate",
    "startCommand": "sleep 5 && npm run start -- -H :: -p $PORT"
  }
}
