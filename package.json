{
  "name": "bgos-railway-root",
  "private": true,
  "scripts": {
    "install": "npm ci --prefix BGOS",
    "build": "npm run build --prefix BGOS",
    "start": "npm run start --prefix BGOS -- -p $PORT"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
