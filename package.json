{
  "name": "7universe-railway-root",
  "private": true,
  "scripts": {
    "install": "npm ci --prefix 7universe-frontend",
    "build": "npx prisma generate --schema 7universe-frontend/prisma/schema.prisma && npm run build --prefix 7universe-frontend",
    "start": "npm run start --prefix 7universe-frontend -- -p $PORT"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
